<%= render Containers::SettingComponent.new(project: @project) do |component| %>
  <% component.with(:body) do %>
    <div>
      <%= render "box_header" %>
    </div>
    <div class="Box">
      <div class="Box-header">
        <h3 class="Box-title">9 labels</h3>
      </div>
      <%= render "label_list" %>
    </div>
  <% end %>
<% end %>